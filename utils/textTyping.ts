import gsap, { splitColor } from "gsap";

export function typeText(text, codeTextWrapper) {
    const splittedText = text.split(' ');
    splittedText.forEach(element => {
        element
    });
    
  };